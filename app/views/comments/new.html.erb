<div class="row">
  <div class="col-sm">
    <h1>New Comment</h1>
  </div>
</div>

<%= render 'form', comment: @comment %>

<div class="row">
  <div class="col-sm mb-3">
    <% comment_links(@comment, current_user, :back).map do |action| %>
        <%= action %>
    <% end %>
  </div>
</div>


